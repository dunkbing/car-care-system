import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 20,
  },
  avatar: {
    flex: 1,
    flexDirection: 'row',
  },
  profileText: {
    paddingLeft: 20,
  },
  option: {
    paddingTop: 30,
  },
});

export default styles;
