export enum STORE_STATES {
  IDLE,
  LOADING,
  LOADING_BACKGROUND,
  SUCCESS,
  ERROR,
}

export enum USER_TYPES {
  CUSTOMER,
  GARAGE_MANAGER,
  GARAGE_STAFF,
}

export enum RESCUE_STATES {
  IDLE,
  PENDING,
  ACCEPTED,
  REJECTED,
  ARRIVED,
  WORKING,
  DONE,
}
