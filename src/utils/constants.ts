export enum STORE_STATES {
  IDLE,
  LOADING,
  LOADING_BACKGROUND,
  SUCCESS,
  ERROR,
}

export enum USER_TYPES {
  CUSTOMER = 0,
  GARAGE_MANAGER = 1,
  GARAGE_STAFF = 2,
}

export enum RESCUE_STATES {
  IDLE,
  PENDING,
  ACCEPTED,
  REJECTED,
  ARRIVED,
  WORKING,
  DONE,
}
